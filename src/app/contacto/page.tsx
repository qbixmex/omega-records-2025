import PublicLayout from "../(public)/public.layout";

export const ContactPage = () => {
  return (
    <PublicLayout>
      <h1 className="text-5xl mt-[150px] text-center">Contacto</h1>
    </PublicLayout>
  );
};

export default ContactPage;

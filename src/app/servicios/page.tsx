import PublicLayout from "../(public)/public.layout";

const ServicesPage = () => {
  return (
    <PublicLayout>
      <h1 className="text-5xl mt-[150px] text-center">Servicios</h1>
    </PublicLayout>
  );
};

export default ServicesPage;

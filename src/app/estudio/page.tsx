import PublicLayout from "../(public)/public.layout";

const StudioPage = () => {
  return (
    <PublicLayout>
      <h1 className="text-5xl mt-[150px] text-center">Estudio</h1>
    </PublicLayout>
  );
};

export default StudioPage;

import PublicLayout from "../(public)/public.layout";

const CoursesPage = () => {
  return (
    <PublicLayout>
      <h1 className="text-5xl mt-[150px] text-center">Cursos</h1>
    </PublicLayout>
  );
};

export default CoursesPage;
